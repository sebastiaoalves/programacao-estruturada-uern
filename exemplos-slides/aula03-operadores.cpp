#include <iostream>

using namespace std;

int main(){ 
    double raio, pi, area;
    area = (cout << "Digite o valor do raio:", cin >> raio, pi=3.14, pi*raio*raio);
    cout << "A área do círculo é : " << area << endl;
    cout << "Tamanho dos dados. Variável raio: " << sizeof(raio) <<  " bytes" << endl;
    cout << sizeof(int) << endl;
    return 0;
}